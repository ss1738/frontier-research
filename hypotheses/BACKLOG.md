# Hypothesis backlog — one experiment/day, rotate domains

Each is a from-scratch small model (1M–~1B params), public/owned
data, with a cheap falsifier. Negatives are logged, not discarded.

| ID | Domain | Hypothesis | Data | Params | Cheap falsifier |
|----|--------|------------|------|--------|-----------------|
| FR-001 | Geophysics | A tiny seq model predicts an imminent ambient-noise dv/v drop (stress precursor) earlier than a persistence/AR baseline | your own seismic-stress `compute_dvv.py` output (already running on the box) | ~1–5M | forward-chained time CV; must beat persistence + AR(p) out-of-sample or NEGATIVE |
| FR-002 | RF / radio | From-scratch tiny model classifies modulation / detects anomaly from raw IQ better than a matched-filter baseline at low SNR | RadioML 2018.01A (public) | ~2–10M | held-out SNR bins; must beat energy/cumulant baseline or NEGATIVE |
| FR-003 | Markets | Small seq model has out-of-sample directional edge on liquid crypto after realistic fees | public Binance 1m klines | ~1–5M | walk-forward, fee-aware; must beat zero-return + AR or NEGATIVE (expected) |
| FR-004 | Genomics | Tiny CNN predicts splice sites from raw sequence on a chromosome-held-out split as well as known baselines | GENCODE/Ensembl public | ~1–5M | chromosome-grouped split; must beat positional/k-mer baseline or NEGATIVE |
| FR-005 | Materials | From-scratch composition-only net predicts formation energy near Roost-class baselines | Materials Project (public API) | ~1–10M | element-held-out split; must beat mean+linear or NEGATIVE |
| FR-006 | Networking | Tiny model flags intrusions from flow features generalizing across attack types | CIC-IDS2017 (public) | ~1–3M | attack-type-held-out; must beat IsolationForest or NEGATIVE |
| FR-012 | Genomics | 85M DNA-GPT on hg38 chr1 learns genomic structure; splice-site embeddings beat 6-mer baseline | hg38 chr1/chr2 | 85M | chr2 ppl < 4.0 AND splice AUC > 6-mer |
| FR-013 | AI/LLM | ICL collapses under sorted-label ordering (all class-0 first, then class-1) because transition statistics—not feature-label association—are the load-bearing mechanism | HuggingFace SST-2, AGNews, TREC, DBPedia, RTE | GPT-2 124M inference only | sorted-label accuracy drops ≥15pp vs random-ordered on 4/5 tasks |
| FR-014 | AI/Physics | PINN multi-scale failure (Helmholtz k=100, stiff Allen-Cahn, Burger's shock, 2D NS) is entirely FP32 precision stalls, not spectral bias or gradient conflict—FP64 alone closes it without architectural change | DeepXDE standard PDE benchmarks | ~500K MLP | FP64 reduces L2 error ≥50% on ≥3/5 canonical failure cases vs FP32-only baseline |
| FR-015 | AI/LLM | Grokking spectral gap (rolling Gram matrix SVD) predicts capability emergence jumps in LLMs ≥1000 steps before the accuracy spike—emergence is grokking at scale, not phase-transition-unpredictable | BabyLM 100M tokens or synthetic 3-hop arithmetic | 50–117M (GPT-2 small) | spectral gap precedes accuracy jump by ≥1000 steps in 5/5 runs |
| FR-016 | AI/CL | LayerNorm drift is the primary mechanism of catastrophic forgetting; freezing LayerNorm params alone (2-line fix) outperforms EWC on Split-CIFAR-100 without any weight regularization | Split-CIFAR-100 (10 tasks) | ViT-Small 22M | frozen-LN avg accuracy ≥ EWC avg accuracy across 10 tasks, 5 seeds |
| FR-017 | AI/Physics | FNO out-of-distribution frequency failure lives entirely in lifting/projection MLPs (not the Fourier spectral conv); swapping those two layers with sinusoidal-conditioned alternatives recovers ≥85% of in-distribution OOD gap | 1D variable-coeff wave equation (train 1–10 Hz, test 10–30 Hz) | 1–5M FNO | modified-FNO OOD error ≤15% relative to in-distribution error |
| FR-018 | BCI/Neuro | Subtracting the subject-specific aperiodic (1/f) EEG component in signal space before any learned encoder sees the data improves LOSO motor-imagery accuracy by ≥10pp over same encoder without this step | PhysioNet EEG MI 109 subjects (T1/T2 left/right fist) | EEGNet ~48K | LOSO accuracy ≥72% vs baseline ~62% (same model, no DA) |
| FR-019 | BCI/Neuro | Microstate transition grammar (treating EEG symbol sequences as a language) > first-order Markov baseline by ≥5pp on cross-subject emotion because transition syntax is more subject-invariant than topographic identity | SEED dataset 15 subjects or DEAP 32 subjects | LSTM/Transformer ~500K | LOSO emotion accuracy ≥ Markov baseline +5pp; trigram > bigram (nested falsifier) |
| FR-020 | BCI/Sleep | A 2-stage cascade where stage-2 uses stage-1 classifier uncertainty (entropy, P(Wake), P(N2)) as additional features raises N1 sleep-stage F1 from ~56% to ≥65% without any change to the core encoder | PhysioNet Sleep-EDF Expanded 153 subjects | Stage1: U-Time 5M; Stage2: MLP 50K | N1 F1 ≥65% on LOSO across 103 held-out subjects |
| FR-021 | Genomics | A codon-level transformer (64-token alphabet) beats ESM2 pseudo-likelihood on ProteinGym multi-mutant epistatic entries from E. coli expression contexts—because co-translational folding shapes epistatic landscapes—but not on yeast-display entries | ProteinGym multi-mutant DMS; UniProt bacterial seqs for pretraining | 15–20M | Spearman Δ≥0.05 vs ESM2 on ≥5/10 E. coli DMS entries; null on yeast-display entries |
| FR-022 | Genomics | A 5M model trained ONLY on inter-motif spatial distances (not TF identity) generalizes across cell types better than sequence models because regulatory spatial grammar is conserved but motif vocabulary is cell-type-specific | ENCODE MPRA (Sharpr-MPRA, lentiMPRA); JASPAR 2024; train K562+HepG2, test GM12878 | 5M transformer on spatial tokens | OOD cell-type correlation > ChromBPNet on held-out MPRA |
| FR-023 | Longevity | Protein evolutionary constraint (ESM2 embeddings of nearest gene) predicts whether a CpG site is in the Horvath aging clock (vs. matched age-correlated CpG)—coupling protein evolution to epigenetic drift | GSE40279 blood methylation; Horvath 353-CpG list; UniProt for evolutionary rates | 2–5M MLP on frozen ESM2 embeds | AUC >0.65 on held-out chromosomes (vs. genomic-features-only baseline) |
| FR-024 | Ocean | SSH anomaly field alone (no chlorophyll input) predicts North Atlantic phytoplankton bloom crash 10–14 days ahead because eddy-driven subduction appears in SSH curvature before the chlorophyll signal collapses | AVISO DUACS daily SSH; MODIS-Aqua chlorophyll-a; train 2003–2016, test 2019–2022 | 3–8M 2D CNN | HSS >0.1 on held-out 2019–2022 bloom seasons; zero-shot to Indian Ocean Arabian Sea as secondary test |
| FR-025 | Ocean | Atlantic-trained eddy mortality model (does this eddy die in 30 days?) generalizes zero-shot to Indian Ocean Agulhas ring shedding at >70% because approach-angle × vorticity geometry is basin-invariant | META3.2 global eddy dataset (AVISO); ERA5 wind; ETOPO1 bathymetry | 10M temporal GNN | Zero-shot Indian Ocean accuracy >70%; open-ocean eddies baseline < boundary-approaching eddy baseline (nested falsifier) |
| FR-026 | Climate | Temperature-conditioned CH4 plume detection (LST as input channel) generalizes zero-shot from summer training to winter test set—improving F1 by ≥5pp—because detection failure is surface-temperature-structured, not terrain-class-structured | EMIT L1B plumes + Carbon Mapper labels; MODIS LST; train Jun–Aug, test Dec–Feb | 5M U-Net | Winter F1 ≥ baseline+5pp; failure mode should correlate with LST not terrain class |

| FR-027 | Materials | A GNN classifying phonon stability from crystal graphs alone achieves ≥88% on random splits but collapses to <65% AUROC on triclinic/monoclinic hold-out—proving it learns symmetry shortcuts, not vibration physics | JARVIS phonon ~14k; Materials Project phonon ~5k | 2–5M CGCNN/ALIGNN | AUROC <65% on triclinic hold-out; coordination-number baseline beats GNN on this split |
| FR-028 | Materials | Superconductor Tc models (RF + GNN) are chemofamily interpolators—family-held-out MAE (cuprates removed) exceeds 20K for both models, exposing that all SOTA Tc prediction is taxonomic memorization, not physics | SuperCon 16k; ICSD Tc subset ~3k CIFs | 3M GNN + RF | Both models MAE >20K on family-held-out; gap ratio >3× vs random-split MAE |
| FR-029 | Materials | Band gap OOD failure on 5 TM elements (Fe/Mn/Co/Ni/Cu) is recoverable: adding explicit d-electron count + spin state as node features to ALIGNN recovers ≥0.10 eV of the OOD gap—proving current GNNs fail because they lack orbital inductive bias | Materials Project band gap 106k structures | 10M orbital-ALIGNN | Orbital-ALIGNN MAE improvement ≥0.10 eV over ALIGNN on TM-held-out split |

| FR-030 | Quantum | A GNN on the circuit DAG predicts barren plateau (gradient variance <1e-6) with AUC >0.85—meaning DLA dimension is predictable in O(n) from circuit syntax, not exponential Lie algebra computation | 3M GNN, synthetic circuits with analytically known DLA dims (PennyLane, n=6–14 qubits) | AUC >0.85 on held-out circuit families; control with shuffled labels AUC ~0.5 |
| FR-031 | Quantum | Non-stabilizerness (magic) of a quantum circuit is predictable from gate-count histogram alone (T-gate count dominates) with SRE MSE <0.05—implying classical/quantum simulation boundary is almost entirely determined by counting T-gates | 5M MLP on gate histogram vs 10M GNN baseline, n=8–16 qubit random circuits | MLP MSE / GNN MSE >0.85 (gate counts explain >85% of what structure explains) |
| FR-032 | Robotics | Contact normal direction error (not visual domain gap, not dynamics mismatch) explains >60% of sim-to-real performance drop in contact-rich manipulation—isolated via 3-way ablation in ManiSkill3 | 5M actor-critic (PPO), ManiSkill3 PegInsertion/TurnFaucet/OpenCabinetDoor | Contact perturbation drop ≥1.5× visual drop AND ≥1.5× dynamics drop |
| FR-033 | Robotics | >80% of cross-sensor tactile generalization failure is illumination map shift (LED ring layout), not elastomer physics—a 500K normalisation head recovers 70%+ with zero new grasp data | 2.5M CNN+head, TACTO synthetic tactile simulator (2×2 factorial: LED×stiffness) | LED-layout drop > elastomer drop by 2×; head recovers ≥70% of gap |
| FR-034 | Robotics | Latent-state curvature (hidden state 2nd derivative over 300ms window) predicts robot grasp failure 1.5s ahead with AUC >0.88—beating action-entropy (FIPER-style) signal by >2pp | 200K probe on frozen 20M ACT policy, RLBench contact-rich tasks | Curvature AUC >0.88 at 1.5s lead; action entropy within 2pp = FAIL |
| FR-035 | Drug Disc. | CYP3A4 time-dependent inactivation kinetics (kinact) are substructure-predictable from 2D SMILES—and that signal alone predicts clinical DDI severity (>2× AUC ratio) better than binary DDI classifiers | 5M AttentiveFP, ChEMBL CYP3A4 TDI data + FDA clinical DDI labels; temporal split | AUC >0.75 on drugs approved post-2020 |
| FR-036 | Drug Disc. | Activity cliff signal lives in delta-fingerprint space (ECFP6_A XOR ECFP6_B), not molecule space—a 1M MLP on this difference vector beats ACGCN cross-target on held-out protein families | 1M MLP, ChEMBL MMP / ACNet benchmark, cross-family scaffold split | Beats ACGCN AUC 0.85 on held-out protein family; null (Tanimoto>0.9) = 0.65 |
| FR-037 | Drug Disc. | Applicability domain gating (Mahalanobis distance from known actives) recovers drug repurposing AUPR from ~0.24 to >0.50 prospectively—the field's 4× retrospective inflation is entirely OOD prediction, not model capacity | 2M ECFP encoder + Mahalanobis gate, Repurposing Hub + TDC, temporal split post-2018 | Prospective AUPR >0.48 after gating; ungated baseline ~0.24 |
| FR-038 | Audio/ASR | Whisper's accent WER gap (Indian/Nigerian/Scottish vs US English) is ≥80% driven by the LM decoder, not the acoustic encoder—a CTC head on the frozen encoder closes ≥30% of that gap with zero acoustic fine-tuning | Common Voice 24 (3 accent splits); frozen Whisper-large-v3 encoder | 3M CTC head | WER gap reduction ≥30% vs full Whisper on accent-held-out split; acoustic-only ablation confirms encoder is near-optimal |
| FR-039 | Audio/Ocean | Cross-ocean cetacean call detector (Atlantic-trained → Pacific test) collapses to <60% AUC—not from call structure differences but ocean ambient noise texture—and a 10-second idle-noise fingerprint as an input condition recovers ≥15pp | NOAA HARP Atlantic (train) + Pacific (test) recordings; MBARI MARS hydrophone noise profiles | 5–15M CNN | Baseline cross-ocean AUC <60%; noise-conditioned model AUC ≥75%; ablation shows fingerprint not call-bank is the lever |
| FR-040 | Audio/Industry | DCASE industrial anomaly domain shift (factory floor) is microphone placement (not RPM or load variation)—a 10-second idle-hum self-calibration channel raises target-domain AUC ≥10pp with zero labeled anomalies | DCASE 2024 Task2 + MIMII DUE; train source domain, test target domain | 3–10M AE/MobileNetV2 | AUC improvement ≥10pp after acoustic self-calibration; RPM-matched ablation shows <3pp from load variation |
| FR-041 | Astrophysics | The 0.89 AUC for single-burst FRB repeater classification (CHIME spectral morphology) collapses to ~0.55 when exposure time, DM, and S/N are matched between repeater and non-repeater samples—the signal is a selection-effect proxy, not intrinsic burst physics | CHIME/FRB Catalog 2 (4,539 bursts, 83 known repeaters); CHIME sky exposure maps | <1M logistic+MLP | Confound-controlled AUC < 0.60 (p < 0.01 permutation); SHAP confirms exposure/DM sneak back into top-3 features |
| FR-042 | Astrophysics | A CNN trained on solar-type (Teff 5000–6000 K) TESS transit light curves drops AUC from >0.95 to <0.70 on M-dwarfs (Teff <4000 K)—not from transit geometry but because flare morphology generates false-positive transit features the solar-trained net systematically misclassifies | TESS TIC v8 + TCE catalog + Cool Dwarf Catalog 370k stars; NASA Exoplanet Archive confirmations | 500K 1D CNN (AstroNet) | M-dwarf AUC <0.70; >50% of M-dwarf false positives co-located with detected flare events (±1 transit duration) |
| FR-043 | Astrophysics | The strong-lens simulation-to-real purity collapse (Euclid: 11% purity) is concentrated in a single confound class—edge-on spiral galaxies—and fine-tuning on 2,000 real spiral negatives recovers purity to >60% while maintaining >65% completeness | KiDS DR5 confirmed lenses (~200) + real spiral negatives; SLACS/BELLS HST lenses; DES test tiles | 5–11M EfficientNet-B0 (fine-tune last 2 layers) | Purity >60% on held-out survey tiles; remaining FPs shift to compact groups/artifacts not spirals (secondary analysis) |
| FR-044 | Fusion | A 2-feature MLP (plasma current rate-of-change + locked mode amplitude) beats the ITER baseline disruption predictor in false-alarm rate at fixed 95% sensitivity—all prior "deep" disruption models add no information over these two signals | C-Mod / JET disruption databases (public subsets via IAEA); train cross-machine | 500K MLP | FPR at 95% sensitivity ≤ baseline; full architecture no better than 2-feature MLP (ablation) |
| FR-045 | Fusion | Mirnov coil time-series (magnetic fluctuation signatures) alone—no Thomson scattering, no soft X-ray—predicts ELM (edge-localized mode) arrival 20ms ahead with AUC >0.80 on shot-held-out ASDEX-U data | ASDEX-Upgrade public ELM-labeled Mirnov data; shot-level hold-out | 2–5M 1D CNN | Shot-hold-out AUC >0.80; beats raw B-dot signal baseline; generalizes to TCV shots without retraining (secondary) |
| FR-046 | Fusion | Pre-shot plasma parameter histogram (Greenwald fraction, q95, βN, triangularity) classifies NIF ignition yield class (below/above 1 MJ) with AUC >0.75 using only public campaign data—target engineering, not implosion physics, is the predictable lever | NIF shot database (public campaign records 2020–2023); ~200 shots with known yields | 200K MLP | AUC >0.75 on 5-fold shot CV; top SHAP feature is from target-engineering not physics parameters |
| FR-047 | Economics | GDELT event-tone time series (geopolitical sentiment) Granger-causes FRED financial volatility indices (VIX, MOVE) with a 2–5 day lag—and a tiny seq model (2M) using only GDELT improves volatility forecast RMSE >10% over ARIMA on held-out 2022–2024 | GDELT 2.0 daily event tone; FRED VIX/MOVE series; train 2015–2021, test 2022–2024 | 2M LSTM | Out-of-sample RMSE improvement >10% vs ARIMA; Granger p < 0.05 at 2–5 day lag; 2023 SVB-crisis period as secondary falsifier |
| FR-048 | Economics | arXiv abstract network position (citation ego-graph centrality at posting time) predicts paper virality (top-10% citations in 3 years) with AUC >0.72—and network position contributes more than abstract text alone, implying social capital drives scientific visibility | arXiv metadata + citation graph (Semantic Scholar OpenCorpus); 2015–2020 papers, 3-year citation window | 3M GNN on citation graph | AUC >0.72 on held-out posting months; graph-only AUC > text-only AUC (nested falsifier) |
| FR-049 | Economics | AIS vessel clustering (voyage pattern similarity) predicts commodity price volatility spikes (crude, grain) 5–10 days ahead because supply disruptions appear in shipping routing before futures markets price them | UN Comtrade + MarineTraffic AIS (public subset); FRED commodity price series; event-based hold-out (Suez/Black Sea incidents) | 5M temporal clustering model | Event-hold-out volatility prediction AUC >0.70; AIS signal leads futures by ≥5 days on 3+ held-out incidents |
| FR-050 | Food/Ag | Soil microbiome beta-diversity (Bray-Curtis distance matrix) from publicly available EMP agricultural samples predicts invasive plant species presence better than abiotic soil chemistry alone (AUC >0.75 vs <0.60)—because invaders restructure microbial communities before visible above-ground signals | Earth Microbiome Project (EMP) agricultural soils; GBIF invasive species occurrence records; site-level hold-out | 2M MLP on microbiome distance features | Site-hold-out AUC >0.75; microbiome beats soil chemistry baseline AUC >0.60 (nested falsifier) |
| FR-051 | Food/Ag | A microbiome-based crop yield predictor trained on one soil type (loam, EMP samples) fails (R² <0.20) on a held-out soil type (clay/sandy)—proving microbiome models learn edaphic proxies not universal yield biology—and genus-level taxonomic aggregation recovers R² >0.50 on cross-soil | EMP agricultural soils (loam/clay/sandy labeled by USDA texture); USDA NASS yield records matched by county | 3M Transformer on genus-level OTU table | Cross-soil R² without aggregation <0.20; genus-aggregated R² >0.50 on held-out soil type |
| FR-052 | Food/Ag | A normalizing flow trained on authentic EVOO (extra virgin olive oil) NIR spectra detects adulteration (sunflower/soybean blends) at ≥5% v/v with AUROC >0.95 and calibrated uncertainty—outperforming PCA-LDA baselines because it models the full density not decision boundaries | EUROFINS EVOO NIR dataset (public challenge data) + synthetic blend spectra; adulteration level held-out | 2M normalizing flow (RealNVP) | AUROC >0.95 at ≥5% adulteration; calibration ECE <0.05; density-threshold beats PCA-LDA F1 on held-out blends |
| FR-053 | Climate | Interannual active-layer thickness (permafrost) variance is dominated by prior-autumn soil moisture state (Sep–Oct VWC), not concurrent thawing degree-days—every current permafrost model conditions on the wrong leading variable | 500K MLP + SHAP, GTN-P CALM network ~150 sites 1990–2024 + ERA5-Land VWC + snow depth | Prior-autumn VWC SHAP contribution > concurrent TDD SHAP at ≥70% of CALM sites with ≥10yr records |
| FR-054 | Climate | AMOC weakening → European extreme precipitation operates through a 6–9 month root-zone soil moisture bridge, not direct SST teleconnection—lagged VWC ablation degrades flood-extreme AUC ≥0.05 while SST ablation degrades <0.03 | 2M LSTM, Caesar AMOC fingerprint + ERA5-Land root-zone VWC + E-OBS extreme precip 1950–2024 | Lagged-VWC ablation AUROC drop ≥0.05 AND SST ablation drop <0.03 |
| FR-055 | Climate | Prior-season global wildfire smoke AOD (MODIS hemispheric asymmetry) predicts NINO3.4 at 6-month lead with ≥0.10°C RMSE improvement over SST-only—aerosol forcing is an ENSO precursor the prediction community has never exploited | 5M 1D CNN, MODIS MOD08_M3 AOD 2001–2024 + NINO3.4 + HadSST4 + ERA5 U850; test 2018–2024 | OOS RMSE improvement ≥0.10°C at 6-month lead vs SST-only baseline |
| FR-056 | Manufacturing | CNC wear model failure cross-material is batch-identity confounding (material hardness variation), not spindle variation—removing top-5 material-correlated frequency bands recovers ≥15pp cross-material F1 | 4M 1D CNN, PHM 2010 CNC Mill Wear dataset; train on materials 1+2, test on material 3 | Cross-material F1 improves ≥15pp after frequency-band ablation vs baseline |
| FR-057 | Manufacturing | Cross-fab semiconductor wafer defect failure is spatial autocorrelation structure shift (die-layout-induced), not defect morphology—variogram whitening of test wafers recovers ≥20pp cross-lot AUC with zero retraining | 5M EfficientNet-B0, WM-811K 811K wafer maps; chronological lot split (lots 1–70% train, 70–100% test) | Cross-lot AUC improves ≥20pp after variogram-trend subtraction; KS p<0.05 on variogram range confirms the shift exists |
| FR-058 | Manufacturing | Neural battery SoH models learn only the trivial capacity fade curve; 3-feature linear model (dQ/dV peak shift + coulombic efficiency Δ + IR at 50% SoC) beats CNN cross-chemistry on CALCE→NASA transfer | 2M 1D CNN vs linear model, CALCE + NASA battery datasets; train on CALCE, test on NASA | Linear model cross-dataset RMSE ≤ CNN cross-dataset RMSE; both ≤5% within-dataset (sanity check) |
| FR-059 | Materials | Copolymer Tg is dominated by monomer sequence order (alternating vs block vs random), not composition ratio—every PolyInfo ML model is composition-conflated and explains ≥0.05 R² less on sequence-stratified hold-out | 8M transformer on BigSMILES vs composition-only head, PolyInfo binary copolymers ~1200–2000 samples with sequence-type labels | BigSMILES head R² > composition-only by ≥0.05 on alternating-vs-block held-out pairs |
| FR-060 | Materials | Li⁺ first-solvation-shell coordination number (CN), not bulk dielectric constant, dominates electrolyte ionic conductivity—Spearman ρ(CN,σ) > ρ(ε,σ)+0.10 on non-carbonate solvents (ethers/sulfones) while ≈equal on carbonates | 15M GNN with/without CN features, OMol25 electrolyte configs + Sriram 2022 experimental conductivities + NIMS electrolyte DB | ρ(CN,σ) > ρ(ε,σ)+0.10 on ether/sulfone subset; within 0.05 on carbonate subset |
| FR-061 | Materials | HEA corrosion models learn SRO through electronegativity proxies that break when SRO is varied at fixed composition—SRO-explicit model (Warren-Cowley params) beats composition-only by ≥0.10 R² on quenched-vs-annealed split | 5M MLP × 2 (composition-only vs composition+WC-SRO), ULTERA + JARVIS-HEA + pitting potential data | SRO-explicit R² > composition-only + 0.10 on processing-stratified test; composition model fails on quenched ≠ annealed pairs |
| FR-062 | Neuroscience | Theta-gamma PAC during working memory does not carry item-specific content—it is a readout artifact of neural geometry transitioning from high to low dimensionality; PAC adds ≤0.05 semi-partial R² to item-decoding beyond geometric dimensionality | 8M TCN encoder + linear probe, Sternberg WM EEG (BNCI Horizon, 52 subjects, 64-channel); PAC vs participation-ratio regression | PAC semi-partial R² ≤ 0.05 (p > 0.01) once geometric dimensionality enters the model across subjects |
| FR-063 | Neuroscience | Visual cortex fMRI power-law covariance spectrum is an input-statistics artifact—random Gaussian projection of the same naturalistic image set reproduces the same spectral exponent within ±0.15; no learned neural computation required | No learned model; SV decomposition, Natural Scenes Dataset 8 subjects 7T fMRI 70K image trials | Random-projection spectral exponent within ±0.15 of fMRI exponent; Wilcoxon p > 0.05 across all 8 subjects |
| FR-064 | Neuroscience | "Attractor dynamics" in iEEG during risky decision deliberation are epoch-locked evoked responses, not autonomous attractor dynamics—ERP template matching achieves within 5pp of LDS decoding accuracy | <5M LDS vs ERP-template decoder, BNCI Horizon gambling EEG 29 subjects 64-channel; cross-trial split | ERP template decoding within 5pp of LDS (paired t p > 0.05); if LDS beats by >5pp, autonomous dynamics confirmed |

---

## Experiment priority queue (post-FR-012)

Ranked by: (speed to answer × novelty × field impact if true)

1. **FR-013** — ICL sorted-label: pure inference, <30min, reshapes ICL theory if true
2. **FR-014** — PINN FP64: ~500K model, 1-2h, falsifies 5 years of architecture work if true
3. **FR-019** — Microstate grammar: 45min, 500K params, genuinely untested cross-field synthesis
4. **FR-018** — EEG aperiodic stripping: 1.5h, crosses two totally isolated communities
5. **FR-024** — SSH bloom crash: 2-3h, proves/disproves eddy subduction as crash mechanism
6. **FR-016** — LayerNorm forgetting: 2h, 2-line fix vs. entire CL literature
7. **FR-017** — FNO lifting fix: 1-3h, localizes FNO's OOD failure to two layers
8. **FR-021** — Codon epistasis: 2-4h, wildest claim in the batch (translation kinetics → epistasis)
9. **FR-025** — Cross-basin eddy: 2-3h, first basin-invariant eddy mortality model
10. **FR-015** — Grokking→emergence: 2-4h, highest stakes if true

---

## Status legend
- DEFINING — hypothesis being written
- KILL-SWITCH — cheap falsification test running
- SCALING — survived kill-switch, larger run
- POSITIVE — survived honest eval + falsifier (candidate; needs independent repro)
- NEGATIVE — falsified honestly (kept on record — this is valuable)
- REPRODUCED — independent second run confirmed it
- RUNNING — training currently active on satyawan-1
- QUEUED — next in queue
- BLOCKED-ON-DATA — harness ready, waiting on data pipeline
